name: phy_interface_vrf
metadata:
    retry_policy:
      init_interval: 1
      backoff_coefficient: 2
      max_interval: 100
      max_attempts: 10
    version: 1.0
dependencies:
  - name: cpe_discovery
    version: 1.2.3
steps:
{% if log_forwarder_present %}
  - name: show_config
    file: show_config.yml

  - name: add_vrf
    file: add_vrf.yml

  - name: show_config_after
    file: show_config_after.yml
{% endif %}
  - name: Fetch_order_configs
    file: Fetch_order_configs.yml

  # - name: clean_up_vrf_config
  #   file: clean_up_vrf_config.yml

  # - name: netconf_vrf_steps
  #   file: netconf_vrf_steps.yml
  #   type: workflow
{% if log_forwarder_present %}
  - name: fetch_routing_instances_after_adding
    file: fetch_routing_instances_after_adding.yml
{% endif %}