name: VPN Provisioning
metadata:
    retry_policy:
      init_interval: 1
      backoff_coefficient: 2
      max_interval: 100
      max_attempts: 10
    version: 1.0
dependencies:
steps:
  - name: PE Configuration
    file: pe_configuration.yml
    type: workflow

  - name: CE Configuration
    file: ce_configuration.yml 
    type: workflow