import { StepTitleDirective } from './step-title.directive';

describe('StepTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new StepTitleDirective();
    expect(directive).toBeTruthy();
  });
});
